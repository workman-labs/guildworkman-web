# Copy to src/.env and fill in real values locally. Never commit src/.env.
# NOTE: Create React App only exposes vars prefixed REACT_APP_ to the client
# bundle; GOOGLE_KEY as named here is not actually read by CRA at build time.
GOOGLE_KEY=
